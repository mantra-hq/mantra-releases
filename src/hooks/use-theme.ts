/**
 * Re-export useTheme hook for cleaner imports
 * Usage: import { useTheme } from '@/hooks/use-theme'
 */
export { useTheme, type Theme, type ThemeProviderContextValue } from "@/lib/theme-provider";

